# Testing environment configuration
# Uses in-memory SQLite for fast, isolated tests

TESTING=True
DEBUG=False

APP_NAME=FastAPI Ecommerce (Test)
APP_VERSION=1.0.0

# Database - will be overridden to in-memory SQLite in conftest.py
DATABASE_URL=sqlite:///:memory:

# JWT Configuration
JWT_SECRET=test-secret-key-change-in-production
JWT_ALG=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15

REFRESH_SECRET=test-refresh-secret-key-change-in-production
REFRESH_TOKEN_EXPIRE_MINUTES=10080

# Email Configuration (mocked in tests)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=test@example.com
SMTP_PASSWORD=test-password
SMTP_FROM=test@example.com

# OTP Configuration
OTP_TTL_SECONDS=600
OTP_RESEND_INTERVAL_SECONDS=60

# Redis Configuration (mocked in tests)
REDIS_URL=redis://localhost:6379/0

# Payment Configuration (mocked in tests)
PAYSTACK_SECRET_KEY=test-paystack-key
PAYSTACK_CALLBACK_URL=http://localhost:8000/payments/paystack/callback

# OAuth Configuration (mocked in tests)
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback
